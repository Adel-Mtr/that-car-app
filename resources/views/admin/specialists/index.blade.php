@extends('layouts.app', ['title' => 'Manage specialists', 'header' => 'Administration · Specialists'])
@section('content')
<div class="mx-auto max-w-[92rem]"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><a href="{{ route('admin.dashboard') }}" class="text-sm font-semibold text-ink-700/55">← Administration</a><h1 class="mt-2 text-3xl font-semibold tracking-[-0.04em]">Specialists</h1><p class="mt-2 text-sm text-ink-700/55">Maintain the service marketplace and verification state.</p></div><a href="{{ route('admin.specialists.create') }}" class="rounded-full bg-ink-950 px-5 py-3 text-center text-sm font-semibold text-white">Add specialist</a></div>
<div class="mt-7 overflow-hidden rounded-card border border-ink-950/8 bg-paper shadow-card"><div class="overflow-x-auto"><table class="w-full min-w-[780px] text-left"><thead class="bg-canvas text-[0.65rem] uppercase tracking-wider text-ink-700/45"><tr><th class="px-5 py-3">Specialist</th><th class="px-5 py-3">Location</th><th class="px-5 py-3">Categories</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Bookings</th><th></th></tr></thead><tbody class="divide-y divide-ink-950/8">@forelse($specialists as $specialist)<tr class="text-sm"><td class="px-5 py-4"><p class="font-semibold">{{ $specialist->name }}</p><p class="mt-1 text-xs text-ink-700/45">{{ $specialist->tagline }}</p></td><td class="px-5 py-4">{{ $specialist->city }}</td><td class="px-5 py-4 text-ink-700/60">{{ implode(', ', array_slice($specialist->categories ?? [], 0, 3)) }}</td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $specialist->is_verified ? 'bg-moss-100 text-moss-700' : 'bg-canvas text-ink-700/55' }}">{{ $specialist->is_verified ? 'Verified' : 'Unverified' }}</span>@if($specialist->is_featured)<span class="ml-1 rounded-full bg-[#f2d749]/35 px-2.5 py-1 text-xs font-semibold">Featured</span>@endif</td><td class="px-5 py-4">{{ $specialist->bookings_count }}</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.specialists.edit', $specialist) }}" class="rounded-full border border-ink-950/10 px-3 py-2 text-xs font-semibold">Edit</a><form method="POST" action="{{ route('admin.specialists.destroy', $specialist) }}" onsubmit="return confirm('Delete this specialist?')">@csrf @method('DELETE')<button class="rounded-full border border-rose-700/15 px-3 py-2 text-xs font-semibold text-rose-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-sm text-ink-700/45">No specialists yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $specialists->links() }}</div></div>
@endsection
